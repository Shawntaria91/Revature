package cookies;

public class HyperCookie extends Cookie {

    private int dimensions;
    public HyperCookie() {
        dimensions = 4;
    }

    // can still use hyperCookie.setEdible because that method has been inherited from cookie
}
